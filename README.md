# archlinux-autonistaller

macmini 2014のarchlinuxインストールを自動化したかった。

# 使い方

インターネットに接続できること。
USBメモリからarchlinuxを起動させて実行するだけ。

* sh base.sh
* sh add_packages.sh

途中rootユーザのパスワード設定する入力があるので入力。
全部終わったら再起動で完了。
